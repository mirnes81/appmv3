{
  "name": "mv3pro_portail_root",
  "version": "1.0.0",
  "private": true,
  "description": "MV3 PRO Portail - Scripts proxy pour la PWA",
  "scripts": {
    "dev": "npm --prefix ./pwa run dev -- --host 0.0.0.0 --port 3100 --strictPort",
    "build": "npm --prefix ./pwa run build",
    "preview": "npm --prefix ./pwa run preview -- --host 0.0.0.0 --port 3100 --strictPort"
  }
}
